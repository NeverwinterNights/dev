import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

type AudioPropsType = {
    
}

const Audio = ({}:AudioPropsType) => {
 return (       
       <View style={styles.container}>
          <Text>Audio</Text>   
        </View>        
    );
};

const styles = StyleSheet.create({
  container: {
  
  }
});


export default Audio
